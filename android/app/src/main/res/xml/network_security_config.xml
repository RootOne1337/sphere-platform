<?xml version="1.0" encoding="utf-8"?>
<!--
    network_security_config.xml — TZ-07 SPLIT-1
    Правило: production запрещает plaintext; dev/localhost разрешён для тестирования.
    https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!-- По умолчанию — только HTTPS, системные CA -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Разрешаем HTTP только к localhost / эмулятору для dev-сборок -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
